<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.store.dao.SeatMapper">

  <sql id="cols">
    id,
    store_id     AS storeId,
    seat_name    AS seatName,
    pos_x        AS posX,
    pos_y        AS posY,
    price_hour   AS priceHour,
    status,
    device_id    AS deviceId,
    create_time  AS createTime,
    update_time  AS updateTime
  </sql>

  <select id="selectById" resultType="com.tencent.wxcloudrun.store.model.Seat">
    SELECT <include refid="cols"/>
    FROM biz_seat
    WHERE id = #{id}
  </select>

  <select id="selectByStoreId" resultType="com.tencent.wxcloudrun.store.model.Seat">
    SELECT <include refid="cols"/>
    FROM biz_seat
    WHERE store_id = #{storeId}
    ORDER BY seat_name
  </select>

  <insert id="insert" parameterType="com.tencent.wxcloudrun.store.model.Seat"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO biz_seat(store_id, seat_name, pos_x, pos_y, price_hour, status, device_id)
    VALUES (#{storeId}, #{seatName}, #{posX}, #{posY}, #{priceHour}, #{status}, #{deviceId})
  </insert>

  <update id="updateById" parameterType="com.tencent.wxcloudrun.store.model.Seat">
    UPDATE biz_seat
    <set>
      <if test="storeId != null">store_id = #{storeId},</if>
      <if test="seatName != null">seat_name = #{seatName},</if>
      <if test="posX != null">pos_x = #{posX},</if>
      <if test="posY != null">pos_y = #{posY},</if>
      <if test="priceHour != null">price_hour = #{priceHour},</if>
      <if test="status != null">status = #{status},</if>
      <if test="deviceId != null">device_id = #{deviceId},</if>
    </set>
    WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM biz_seat WHERE id = #{id}
  </delete>

  <delete id="deleteByStoreId">
    DELETE FROM biz_seat WHERE store_id = #{storeId}
  </delete>

  <select id="countByStoreId" resultType="int">
    SELECT COUNT(*) FROM biz_seat WHERE store_id = #{storeId}
  </select>

</mapper>
